PORT=8080
DATABASE_URL=postgres://spec_first:spec_first@localhost:5432/spec_first?sslmode=disable
JWT_SECRET=change-me-in-production
# Comma-separated list of allowed CORS origins, e.g. for a frontend dev
# server on a different port. Leave empty/unset to disable CORS entirely.
CORS_ORIGINS=http://localhost:5173,http://localhost:5174
